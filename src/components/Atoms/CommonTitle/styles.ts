import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  titleWrapper: {
    marginBottom: 32,
  },
  title: {
    color: "#000",
    fontSize: 32,
    fontWeight: "500",
    lineHeight: 38,
    letterSpacing: -0.408,
    marginBottom: 12,
  },
  subtitle: {
    color: "rgba(0, 0, 0, 0.50)", // Use the desired color
    fontSize: 15,
    fontWeight: "400",
    lineHeight: 18,
    letterSpacing: -0.408,
  },
});

export default styles;
